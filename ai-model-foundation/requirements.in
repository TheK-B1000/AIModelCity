# Base dependencies for pip-compile. Regenerate lockfile with:
#   pip install pip-tools && pip-compile requirements.in -o requirements.txt
pyyaml>=6.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
mlflow>=2.9
