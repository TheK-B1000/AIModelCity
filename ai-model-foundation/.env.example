# Copy to .env and set real values. Never commit .env.
# Required only when using MLflow or external services.

# Registry (MLflow) — optional; defaults to local ./registry
# FOUNDATION_REGISTRY_URI=http://127.0.0.1:5000
# FOUNDATION_REGISTRY_BACKEND=mlflow

# MLflow tracking (if using Registry Hall)
# MLFLOW_TRACKING_URI=http://127.0.0.1:5000

# Database or storage URLs — if you add them later, use env only
# DATABASE_URL=
# TRAIN_DATA_URI=

# API keys — never put in config files
# API_KEY_OPENAI=
# API_KEY_ANTHROPIC=
